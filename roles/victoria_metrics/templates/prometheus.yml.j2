global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'proxmox-lxc-nodes'
    static_configs:
      - targets:
{% for ct in containers %}
        - '{{ ct.hostname }}.home:9100' # default port
{% endfor %}